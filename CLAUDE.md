# CLAUDE.md

This file provides guidance to Claude Code when working in this repository.

## Project Overview

This is the Sandbox repository. <!-- Update with a brief project description -->

## Build & Run Commands

<!-- Update these with actual project commands -->
- **Install dependencies**: `npm install` / `pip install -r requirements.txt`
- **Run**: `npm start` / `python main.py`
- **Test**: `npm test` / `pytest`
- **Lint**: `npm run lint` / `ruff check .`
- **Single test**: `npm test -- --grep "test name"` / `pytest path/to/test.py::test_name`

## Code Style & Conventions

- Follow existing patterns and conventions in the codebase
- Keep changes minimal and focused on the task at hand
- Prefer clear, readable code over clever abstractions
- Use descriptive variable and function names

## Git Conventions

- Write concise commit messages that explain *why*, not just *what*
- Keep commits focused on a single logical change

## Key Directories

<!-- Update with actual project structure -->
<!-- Example:
- `src/` — Application source code
- `tests/` — Test files
- `docs/` — Documentation
-->
